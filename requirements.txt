# Core ML frameworks
torch>=2.7.0
transformers>=4.57.0
accelerate>=1.12.0

# For GPT-2 logit lens (TensorFlow 1.x - use on Lambda/GPU only)
# tensorflow==1.15  # Uncomment for GPT-2 notebook

# Image processing (IMPORTANT: need >=9.1.0 for PIL.Image.Resampling required by transformers)
Pillow>=9.1.0

# Visualization
matplotlib>=3.5.0
colorcet>=3.0.0

# Data processing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0

# HuggingFace datasets (IMPORTANT: <3.0.0 for EDGAR corpus loading script support)
datasets>=2.14.0,<3.0.0

# Progress bars
tqdm>=4.62.0

# HuggingFace CLI (for model downloads)
huggingface-hub>=0.16.0

# HTTP requests (for Lambda API)
requests>=2.28.0
